# Example environment file for AWS Cloud Map Prometheus Service Discovery API
# Copy this file to .env and customize the values for your environment

# Server Configuration
HOST=0.0.0.0
PORT=3030
RUST_LOG=info

# AWS Configuration
AWS_REGION=us-west-2
AWS_PROFILE=default

# Optional: AWS Credentials (if not using profiles or IAM roles)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token

# Optional: Filter by specific Cloud Map namespace
# Leave commented to discover all namespaces
# CLOUDMAP_NAMESPACE=production

# Docker Configuration
REGISTRY=
TAG=latest

# Development Configuration
# Uncomment for development mode
# RUST_LOG=debug
